export interface Test1 {
    id: number; // 自動インクリメントされるID
    name: string; // 名前
    createdAt: Date; // 作成日時
}

export interface Test2 {
    id: number; // 自動インクリメントされるID
    name: string; // 名前
    createdAt: Date; // 作成日時
}